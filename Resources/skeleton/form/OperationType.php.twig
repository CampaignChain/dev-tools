{% if module_type == 'operation' %}
<?php

namespace {{ namespace|replace({'\\\\': '\\'}) }}\Form\Type;

use CampaignChain\CoreBundle\Form\Type\OperationType;
use Symfony\Component\Form\FormBuilderInterface;
use Symfony\Component\OptionsResolver\OptionsResolverInterface;

class {{ class_name }}OperationType extends OperationType
{
    public function buildForm(FormBuilderInterface $builder, array $options)
    {

    }

    public function setDefaultOptions(OptionsResolverInterface $resolver)
    {
        $defaults = array();
        $resolver->setDefaults($defaults);

        /*
        Delete above code and use optional code below.

        $defaults = array(
            'data_class' => '{{ namespace|replace({'\\\\': '\\'}) }}\Entity\{{ class_name }}',
        );

        if($this->content){
            $defaults['data'] = $this->content;
        }
        $resolver->setDefaults($defaults);
        */
    }

    public function getName()
    {
        return '{{ vendor_name|lower }}_operation_{{ module_name|lower|replace({'-':'_'}) }}{{ module_name_suffix ? '_' ~ module_name_suffix|lower|replace({'-':'_'}) : '' }}';
    }
}
{% endif %}
